{
  "name": "andrepatrinicola.com",
  "version": "0.1.0",
  "private": true,
  "description": "Monorepo pessoal de André de Queiroz.",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace @andrepatrinicola/site",
    "build": "npm run build --workspace @andrepatrinicola/site",
    "preview": "npm run preview --workspace @andrepatrinicola/site",
    "lint": "npm run lint --workspace @andrepatrinicola/site"
  },
  "engines": {
    "node": ">=20"
  }
}
